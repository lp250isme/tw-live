# Base URL of the tw-live Worker data proxy.
# Local dev (run the worker with: cd worker && npm run dev -- --port 8799):
#   VITE_API_BASE=http://127.0.0.1:8799
# Production is set in Vercel project env:
#   VITE_API_BASE=https://live-api.kvcc.me
VITE_API_BASE=https://live-api.kvcc.me
